export {
  durations,
  easings,
  spacing,
  colors,
  shadows,
  borderRadius,
  typography,
  layout,
  buildTransition,
} from "./tokens";

export type { Duration, Easing, Shadow, SpacingKey } from "./tokens";
